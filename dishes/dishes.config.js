const diet = ["Vegetarian", "Non-vegetarian"];

const flavorprofile = ['Sweet', 'Spicy', 'Bitter', 'Sour'];

const course = ['Dessert', 'Main course', 'Starter', 'Snack'];

const region = ['East', 'West', 'North', 'North East', 'South', 'Central'];

const status = ['Active', 'Inactive'];

module.exports = {
  diet,
  flavorprofile,
  course,
  region,
  status
}